# 🧪 Тесты

Тестовые файлы для проверки работы парсера.

## 📁 Файлы

### test-no-translate.js
Тестирование работы без перевода (быстрый режим).

**Запуск:**
```bash
npm test
# или
node tests/test-no-translate.js
```

**Что тестирует:**
- Нормализацию слов
- Подсчет частоты
- Работу без перевода
- Корректность результатов

### test-min-frequency.js
Тестирование фильтрации по минимальной частоте.

**Запуск:**
```bash
npm run test:freq
# или
node tests/test-min-frequency.js
```

**Что тестирует:**
- Фильтрацию слов по минимальной частоте
- Различные значения --min-freq
- Корректность подсчета
- Практическое применение

## ✅ Запуск тестов

```bash
# Основной тест
npm test

# Тест фильтрации по частоте
npm run test:freq

# Демонстрация (тоже вид теста)
npm run demo
```

## 📊 Ожидаемый результат

```
=== Тест без перевода ===

📊 Результаты анализа:

Найдено уникальных слов: 19

Топ-10 слов по частоте:

   1. quick - 2 раз
   2. fox - 2 раз
   3. jump - 2 раз
   ...

✅ Тест завершен!
```

## 💡 Добавление своих тестов

Вы можете добавить свои тестовые файлы:

```javascript
// tests/my-test.js
const { WordProcessor } = require('../index.js');

console.log('=== Мой тест ===');

const processor = new WordProcessor();
processor.processText('Test text here');

const words = processor.getSortedWords();
console.log('Результат:', words);

console.log('✅ Тест пройден!');
```

Запуск:
```bash
node tests/my-test.js
```

## 🔧 Обновление package.json

Чтобы добавить свой тест в npm:

```json
"scripts": {
  "test": "node tests/test-no-translate.js",
  "test:my": "node tests/my-test.js"
}
```

Запуск:
```bash
npm run test:my
```

## 📚 Дополнительно

Больше информации в [документации](../docs/).


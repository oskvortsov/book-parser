# 📝 Примеры использования

Здесь находятся примеры и демонстрации работы парсера.

## 📁 Файлы

### demo.js
Демонстрация работы нормализации и лемматизации слов.

**Запуск:**
```bash
npm run demo
# или
node examples/demo.js
```

**Что показывает:**
- Нормализация разных форм глаголов
- Приведение к нижнему регистру
- Удаление знаков препинания
- Фильтрация стоп-слов
- Подсчет частоты

### example.js
Примеры использования парсера как модуля в своем коде.

**Использование:**
```javascript
const { parseEpubBook, WordProcessor, translateWords } = require('../index.js');

// Пример 1: Парсинг книги
const processor = await parseEpubBook('./book.epub');
const words = processor.getSortedWords();

// Пример 2: Перевод слов
const translated = await translateWords(words, 10);

// Пример 3: Кастомная обработка
const customProcessor = new WordProcessor();
customProcessor.processText('Your text here');
```

## 🚀 Быстрый старт с примерами

### 1. Демонстрация нормализации
```bash
npm run demo
```

Вы увидите:
- Как "running, runs, ran" превращаются в "run"
- Как "Book, BOOK, book" становятся одним словом
- Как фильтруются стоп-слова

### 2. Использование как модуль

Смотрите код в `example.js` для интеграции в свой проект.

## 💡 Создание своих примеров

Вы можете создать свои примеры в этой папке:

```javascript
// examples/my-example.js
const { WordProcessor } = require('../index.js');

const processor = new WordProcessor();
processor.processText('Your custom text here');

const words = processor.getSortedWords();
console.log('Top 5 words:', words.slice(0, 5));
```

## 📚 Дополнительно

Больше информации в [документации](../docs/).


# 📋 Описание проекта

## Структура проекта

```
book-parser/
├── index.js           # Основной файл программы
├── demo.js            # Демонстрация нормализации слов
├── example.js         # Примеры использования как модуля
├── package.json       # Конфигурация npm
├── README.md          # Полная документация
├── QUICKSTART.md      # Быстрый старт
└── .gitignore         # Игнорируемые файлы
```

## Основные компоненты

### 1. WordProcessor (класс)
Отвечает за обработку текста и подсчет частоты слов.

**Методы:**
- `normalizeWord(word)` - нормализует слово (lowercase, stemming, фильтрация)
- `processText(text)` - обрабатывает текст и подсчитывает слова
- `getSortedWords()` - возвращает отсортированный массив слов

**Особенности:**
- Использует Porter Stemmer для приведения к базовой форме
- Фильтрует стоп-слова (the, a, is, etc.)
- Удаляет слова короче 3 символов
- Приводит к lowercase

### 2. parseEpubBook(epubPath)
Асинхронная функция для парсинга EPUB файлов.

**Возвращает:** Promise<WordProcessor>

**Процесс:**
1. Открывает EPUB файл
2. Читает все главы
3. Очищает от HTML тегов
4. Обрабатывает текст через WordProcessor
5. Возвращает обработанный процессор

### 3. translateWords(words, maxWords)
Переводит список слов с английского на русский.

**Параметры:**
- `words` - массив объектов {word, count}
- `maxWords` - максимальное количество слов для перевода (по умолчанию 100)

**Возвращает:** Promise<Array<{original, translation, count}>>

**Особенности:**
- Использует Google Translate API (бесплатный)
- Добавляет задержку между запросами (100ms)
- Обрабатывает ошибки перевода

### 4. saveResults(words, outputPath)
Сохраняет результаты в JSON и TXT форматы.

**Создает:**
- `filename_words.json` - полная статистика
- `filename_words.txt` - читаемый текстовый формат

## Алгоритм работы

```
1. Парсинг EPUB
   ↓
2. Извлечение текста из глав
   ↓
3. Токенизация (разбивка на слова)
   ↓
4. Нормализация каждого слова:
   - toLowerCase()
   - Удаление пунктуации
   - Проверка на стоп-слова
   - Проверка длины (>= 3)
   - Stemming (Porter)
   ↓
5. Подсчет частоты
   ↓
6. Сортировка по убыванию частоты
   ↓
7. Перевод топ-N слов
   ↓
8. Сохранение результатов
```

## Используемые технологии

### Natural (NLP библиотека)
- **WordTokenizer** - разбивает текст на слова
- **PorterStemmer** - приводит слова к основе (stem)

**Примеры работы стемминга:**
- running, runs, ran → run
- looking, looked, looks → look
- books → book

### epub2
- Парсинг EPUB файлов
- Извлечение глав и метаданных
- Обработка HTML содержимого

### @iamtraction/google-translate
- Бесплатный доступ к Google Translate
- Поддержка множества языков
- Простой API

## Ограничения и известные проблемы

### 1. Стемминг не идеален
Porter Stemmer иногда дает неожиданные результаты:
- "better" → "bet"
- "worse" → "wors"

**Решение:** Для более точной лемматизации можно использовать WordNet (требует дополнительной настройки)

### 2. Только английский язык
Программа оптимизирована для английского.

**Решение:** Для других языков нужно:
- Изменить список стоп-слов
- Использовать соответствующий стеммер/лемматизатор

### 3. Скорость перевода
Google Translate API может быть медленным для большого количества слов.

**Решение:** 
- Уменьшить количество переводимых слов
- Использовать платный API для быстрого перевода

### 4. Ограничения API перевода
Бесплатный Google Translate может блокировать при большом количестве запросов.

**Решение:**
- Добавлены задержки между запросами
- Перевод выполняется порциями

## Расширение функциональности

### Добавить поддержку других форматов

```javascript
// В index.js добавить:
async function parsePdfBook(pdfPath) {
  // Реализация парсинга PDF
}
```

### Использовать WordNet для лучшей лемматизации

```javascript
const natural = require('natural');
const WordNet = new natural.WordNet();

WordNet.lookup('running', (results) => {
  // Получить лемму
});
```

### Добавить экспорт в Anki

```javascript
function exportToAnki(words) {
  // Создание .apkg файла для Anki
}
```

### Добавить GUI

Можно создать веб-интерфейс с помощью Electron или веб-сервер с Express.

## Тестирование

Запустите демонстрацию:
```bash
npm run demo
```

Или используйте тестовый текст:
```javascript
const { WordProcessor } = require('./index.js');
const processor = new WordProcessor();
processor.processText('Your test text here');
const words = processor.getSortedWords();
console.log(words);
```

## Производительность

Примерное время обработки:
- Книга 100 страниц: ~5-10 секунд (парсинг)
- Перевод 100 слов: ~15-20 секунд
- Перевод 500 слов: ~1-2 минуты

**Зависит от:**
- Размера книги
- Скорости интернета
- Загруженности Google Translate API

## Вклад в проект

Идеи для улучшения:
- [ ] Поддержка других языков
- [ ] Использование WordNet для лемматизации
- [ ] Экспорт в Anki
- [ ] Веб-интерфейс
- [ ] Поддержка PDF, TXT, MOBI
- [ ] Кэширование переводов
- [ ] Статистика сложности текста
- [ ] Визуализация частотности (графики)
- [ ] CLI с прогресс-баром

## Лицензия

MIT


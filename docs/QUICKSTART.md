# 🚀 Быстрый старт

## Установка

```bash
npm install
```

## Использование

### 1. Простой запуск

```bash
node index.js путь/к/книге.epub
```

Это обработает книгу и переведет топ-100 слов.

### 2. С настройкой количества слов

```bash
node index.js путь/к/книге.epub 200
```

Переведет топ-200 самых частых слов.

### 3. Без перевода (быстрый режим)

```bash
node index.js путь/к/книге.epub 0
# или
node index.js путь/к/книге.epub --no-translate
```

Быстрый анализ без перевода - полезно для больших книг.

### 4. Фильтрация по частоте

```bash
# Только слова, встречающиеся минимум 5 раз
node index.js путь/к/книге.epub 100 --min-freq 5

# Только очень частые слова (>= 10 раз), без перевода
node index.js путь/к/книге.epub --no-translate --min-freq 10
```

Полезно для фокусировки на самых важных словах.

### 3. Пример с реальной книгой

```bash
# Скачайте любую бесплатную книгу в формате EPUB
# Например, с Project Gutenberg (https://www.gutenberg.org/)

node index.js ~/Downloads/alice-in-wonderland.epub 150
```

## Результат

Программа создаст два файла:
- `alice-in-wonderland_words.json` - JSON с полной статистикой
- `alice-in-wonderland_words.txt` - текстовый файл для удобного просмотра

## Что показывает программа

```
📖 Найдено глав: 12
✓ Обработана глава 1/12
...
✓ Обработана глава 12/12

📊 Статистика:
   Уникальных слов: 2534
   Всего слов: 26432

🔝 Топ-10 самых частых слов:
   1. said - 462 раз
   2. alice - 398 раз
   3. little - 128 раз
   ...

🔤 Начинаем перевод топ-150 слов...
✓ Переведено 10/150 слов
...
✓ Переведено 150/150 слов

💾 Результаты сохранены в: alice-in-wonderland_words.json
📄 Текстовая версия: alice-in-wonderland_words.txt

✅ Готово!
```

## Тестирование без книги

Чтобы протестировать программу без реальной книги, используйте пример:

```javascript
const { WordProcessor } = require('./index.js');

const processor = new WordProcessor();
processor.processText('Running runs runner. Better best. Looking looked looks.');

const words = processor.getSortedWords();
console.log(words);
// Результат: слова в базовой форме с частотой
```

## Где взять бесплатные EPUB книги

- **Project Gutenberg**: https://www.gutenberg.org/ (классическая литература)
- **Open Library**: https://openlibrary.org/
- **ManyBooks**: https://manybooks.net/
- **Feedbooks**: https://www.feedbooks.com/publicdomain

## Советы

1. **Для изучения языка**: Используйте книги соответствующего уровня сложности
2. **Оптимизация**: Для больших книг процесс перевода может занять время - используйте `--no-translate` для быстрого анализа
3. **Частота встречаемости**: Самые частые слова обычно самые полезные для изучения
4. **Экспорт**: JSON файл можно импортировать в анки-карточки или другие приложения
5. **Быстрый анализ**: Сначала запустите без перевода, посмотрите статистику, затем переведите нужное количество слов

## Проблемы?

Если возникли проблемы:

1. Убедитесь, что установлены зависимости: `npm install`
2. Проверьте, что файл EPUB существует и путь указан правильно
3. Убедитесь, что у вас есть интернет (для перевода)
4. Проверьте версию Node.js: `node --version` (нужна >= 12.0.0)

